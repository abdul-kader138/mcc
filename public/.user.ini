; Upload limits for GLB models and cover images.
; Keep post_max_size larger than upload_max_filesize so Livewire metadata is included.
upload_max_filesize=128M
post_max_size=150M
max_execution_time=300
max_input_time=300
